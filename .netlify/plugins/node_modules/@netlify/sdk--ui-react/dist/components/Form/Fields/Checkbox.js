import { jsx } from "react/jsx-runtime";
import { u as useFormContext, C as Controller } from "../../../index.esm-BuArIOt4.js";
import BaseCheckbox from "../../vendor/Checkbox.js";
const Checkbox = ({
  // Ignore this value when passed so it doesn't end up in the `otherProps` rest
  value: _value,
  name,
  label,
  helpText,
  error,
  required,
  ...otherProps
}) => {
  const { control } = useFormContext();
  return /* @__PURE__ */ jsx(
    Controller,
    {
      name,
      control,
      render: ({ field, fieldState }) => {
        var _a;
        return /* @__PURE__ */ jsx(
          BaseCheckbox,
          {
            label,
            controlledChecked: Boolean(field.value),
            helpText,
            error: (error == null ? void 0 : error.message) ?? ((_a = fieldState.error) == null ? void 0 : _a.message),
            required,
            ...otherProps,
            ...field,
            value: field.value
          }
        );
      }
    }
  );
};
export {
  Checkbox
};
//# sourceMappingURL=Checkbox.js.map
